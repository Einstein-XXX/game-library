# GameLibrary Backend Configuration

# Server Configuration
server.port=8080
server.servlet.context-path=/api

# MongoDB Configuration - Using Embedded MongoDB (no installation needed!)
de.flapdoodle.mongodb.embedded.version=6.0.9
spring.data.mongodb.database=gamelibrary

# MongoDB logging
logging.level.org.springframework.data.mongodb=INFO
logging.level.org.springframework.data.mongodb.core=DEBUG

# JWT Configuration
jwt.secret=Y29tcGxleF9zZWNyZXRfa2V5X2Zvcl9nYW1lX2xpYnJhcnlfYXBwbGljYXRpb25fMjAyNQ==
jwt.expiration=86400000

# Google OAuth2 Configuration
# Credentials from Google Cloud Console (Created: Dec 3, 2025)
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:YOUR_GOOGLE_CLIENT_ID_HERE}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET:YOUR_GOOGLE_CLIENT_SECRET_HERE}
spring.security.oauth2.client.registration.google.scope=profile,email
spring.security.oauth2.client.registration.google.redirect-uri={baseUrl}/login/oauth2/code/{registrationId}

# CORS Configuration
cors.allowed-origins=http://localhost:5173,http://localhost:5174,http://localhost:3000
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=*
cors.allow-credentials=true

# RAWG API Configuration (for fetching game data)
rawg.api.key=9bb25de7001443d096ea366e1e990de1
rawg.api.base-url=https://api.rawg.io/api

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Logging Configuration
logging.level.com.gamelibrary=DEBUG
logging.level.org.springframework.security=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

# Application Info
app.name=GameLibrary
app.version=1.0.0
app.description=Game Store Backend API

# Admin User Configuration
# These settings control the default admin user that is created on startup
# Set app.admin.auto-create=false to disable automatic admin creation
app.admin.auto-create=true
app.admin.email=abdulbasitafzal33@gmail.com
app.admin.username=admin
app.admin.password=admin123

